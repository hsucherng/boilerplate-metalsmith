module.exports = {
    /*
     * This is to programmatically place the build files inside a given path.
     * For example, if the virtualFolder's value is set to `the-jackson-fives`,
     * the files will be served over `localhost:8080/the-jackson-fives/`.
     */
    virtualFolder: 'site'
};